# Auxiliar shortcut scales with opinionated pre-sets

Auxiliar shortcut scales with opinionated pre-sets

## Usage

``` r
scale_x_date_year(date_labels = "%Y", ...)

scale_x_date_yyyymm(date_labels = "%Y%m", ...)
```

## Arguments

- date_labels:

  Date labels

- ...:

  More arguments for \`scale_x_date\`
