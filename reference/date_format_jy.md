# A date format helper to show the year in January

A date format helper to show the year in January

## Usage

``` r
date_format_jy(x, date_label = "%b")
```

## Arguments

- x:

  vector of date class

- date_label:

  the formar of date
